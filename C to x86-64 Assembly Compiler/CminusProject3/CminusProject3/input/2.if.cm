#include <stdio.h>

int a;

int main() 
{
   printf("enter a:");
   scanf("%d", &a);
   
   if (a) { 
     printf("%d is not 0\n", a);
   } else {
     printf("%d is 0\n", a);
   }
    printf("complete\n");
}
