#include <stdio.h>

int a,b;
	
int main() {	
  printf("enter a:");
  scanf("%d", &a);
  printf("enter b:");
  scanf("%d", &b);
	
  if (b > a) { 
    if (a > 0) {
      printf("a = %d\n", a);
    }
    else {
      printf("b = %d\n", b);
    }
  }
  else {
    if (a < 0) {
      printf("a = %d\n", a);
    }
    else {
      printf("b = %d\n", b);
    }
  }
}
